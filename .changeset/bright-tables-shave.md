---
'@window-manager/core': patch
'@window-manager/react': patch
---

Fix hydrated state sanitization in the core package, keep oversized created/resized windows inside monitor bounds, align the playground persistence path with the core serializer, and prepare the workspace packages for changesets-based releases.
